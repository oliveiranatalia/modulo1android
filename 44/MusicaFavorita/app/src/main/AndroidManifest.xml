<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="br.com.zup.musicafavorita">

    <application
        android:allowBackup="true"
        android:icon="@drawable/album_oito"
        android:label="@string/app_name"
        android:roundIcon="@drawable/album_oito"
        android:supportsRtl="true"
        android:theme="@style/Theme.MusicaFavorita">
        <activity
            android:name=".albumDetail.AlbumDetailsActivity"
            android:exported="false" />
        <activity
            android:name="br.com.zup.musicafavorita.viewPager.InfoActivity"
            android:exported="false"
            android:label="@string/title_activity_info" />
        <activity
            android:name=".home.HomeActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>